{
  "name": "wedding-poc-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted Fastify + SQLite backend for the wedding venue app POC",
  "engines": {
    "node": ">=20.0.0 <23.0.0",
    "//": "Node 20 or 22 LTS recommended. better-sqlite3 ships prebuilt binaries only for LTS versions, so Node 23/24/25 (Current line) will try to compile from C++ source and fail without Visual Studio Build Tools on Windows."
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "migrate": "tsx src/db/migrate.ts",
    "seed": "tsx src/db/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/jwt": "^8.0.1",
    "@fastify/static": "^7.0.4",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.5.5",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
