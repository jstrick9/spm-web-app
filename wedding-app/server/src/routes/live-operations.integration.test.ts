import './../test/setup.js'; import { describe,it,expect,beforeAll,beforeEach } from 'vitest'; import { buildApp } from '../index.js'; import { db } from '../db/database.js'; import type { FastifyInstance } from 'fastify'; let app:FastifyInstance;beforeAll(async()=>{app=await buildApp();await app.ready();});beforeEach(()=>{for(const t of ['staff_tasks','staff_shifts','vendors','layouts','timeline_incidents','events','organization_memberships','organizations','users']){try{db.prepare(`DELETE FROM ${t}`).run();}catch{}}});describe('Live operations board',()=>{it('aggregates event-week operational work for venue leaders',async()=>{const r=await app.inject({method:'POST',url:'/api/auth/register',payload:{email:`live-${Math.random()}@test.com`,password:'password123',fullName:'Manager',orgName:'Seven Paths Manor'}});const token=r.json().token,orgId=r.json().organizationId;const e=await app.inject({method:'POST',url:'/api/events',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},payload:{organizationId:orgId,title:'Live Wedding'}});const id=e.json().event.id;await app.inject({method:'POST',url:`/api/orgs/${orgId}/staff/tasks`,headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},payload:{eventId:id,title:'Set ceremony chairs',priority:'high'}});const res=await app.inject({method:'GET',url:`/api/events/${id}/live-operations`,headers:{authorization:`Bearer ${token}`}});expect(res.statusCode).toBe(200);expect(res.json().board.event.title).toBe('Live Wedding');expect(res.json().board.tasks[0].title).toBe('Set ceremony chairs');});});
