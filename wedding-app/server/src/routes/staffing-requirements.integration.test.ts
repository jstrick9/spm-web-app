import './../test/setup.js'; import { describe,it,expect,beforeAll,beforeEach } from 'vitest'; import { buildApp } from '../index.js'; import { db } from '../db/database.js'; import type { FastifyInstance } from 'fastify'; let app:FastifyInstance;beforeAll(async()=>{app=await buildApp();await app.ready();});beforeEach(()=>{for(const t of ['events','organization_memberships','organizations','users']){try{db.prepare(`DELETE FROM ${t}`).run();}catch{}}});describe('Staffing requirements',()=>{it('lets venue managers configure roles required per event',async()=>{const r=await app.inject({method:'POST',url:'/api/auth/register',payload:{email:`roles-${Math.random()}@test.com`,password:'password123',fullName:'Manager',orgName:'Seven Paths Manor'}});const token=r.json().token,orgId=r.json().organizationId;const e=await app.inject({method:'POST',url:'/api/events',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},payload:{organizationId:orgId,title:'Requirements Wedding'}});const id=e.json().event.id;const saved=await app.inject({method:'PUT',url:`/api/events/${id}/staffing-requirements`,headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},payload:{requiredRoles:['coordinator','parking']}});expect(saved.statusCode).toBe(200);const got=await app.inject({method:'GET',url:`/api/events/${id}/staffing-requirements`,headers:{authorization:`Bearer ${token}`}});expect(got.json().requiredRoles).toEqual(['coordinator','parking']);});});
